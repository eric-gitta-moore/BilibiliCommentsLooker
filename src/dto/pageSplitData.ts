export interface PageSplitData {
  all_count: number;
  is_begin: boolean;
  prev: number;
  next: number;
  is_end: boolean;
  mode: number;
  show_type: number;
  support_mode: Array<number>;
  name: string;
}
